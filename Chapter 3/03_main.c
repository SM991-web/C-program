#include <stdio.h>

int main(){
    int age = 40;
    if (age>=18 , age >=45)
    {
        printf("You are eligible to vote");
    }
    else if (age != 18)
    {
        printf("You are eligible to vote");
    }
    return 0;

}