#include <stdio.h>

int main(){
    int radius;
    scanf("%d", &radius);
    printf("Enter the value of radius");
    printf("The area of the circle is: %f", 3.14 * radius * radius);
    
    return 0;
}
