#include <stdio.h>

int main()
{
    printf("The size of int: %zu bytes\n", sizeof(int));
    printf("The size of float: %zu bytes\n", sizeof(float));
    printf("The size of char: %zu bytes\n", sizeof(char));

    return 0;
}
